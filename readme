build
-----

Minimal build tool for POSIX systems, designed to be simple & straightforward.
For more information read the manpage after installation (man build).

Install:
    $ su -c target/bootstrap.sh
